{
    "schemaVersion": 1,
    "decision": "accepted",
    "reason": "The Admin zone context signature is an intentional compatibility-preserving addition: the context now carries the current actor and an optional surface subject.",
    "drift": ["changed public signature: admin.dto.admin-zone-context"]
}
